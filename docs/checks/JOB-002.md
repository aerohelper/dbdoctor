ID: JOB-002

Name:
Job is repeatedly failing

Severity:
CRITICAL

Why:
A job that repeatedly fails often signals a broken pipeline rather than transient issues,
and deserves immediate attention.

Detection:
`recentFailureCount >= 3` (default threshold, configurable) among the 10 most recently
collected runs. Jobs whose run history couldn't be fetched are skipped rather than flagged.

Recommendation:
Investigate and fix the underlying cause of these failures.

Can auto-fix:
No
