ID: SQL-001

Name:
Auto-stop disabled

Severity:
CRITICAL

Why:
An idle SQL warehouse that never stops keeps accruing DBU cost with no query traffic to
justify it.

Detection:
`autoStopMins == 0` or unset (`null`).

Recommendation:
Configure auto-stop on the warehouse.

Can auto-fix:
No
